tasty_bytes:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: 'not needed'
      user: 'not needed'
      role: TRAINING_ROLE
      database: TASTY_BYTES_DBT_DB
      schema: dev
      warehouse: ANIMAL_TASK_WH
    prod:
      type: snowflake
      account: 'not needed'
      user: 'not needed'
      role: TRAINING_ROLE
      database: tasty_bytes_dbt_db
      schema: prod
      warehouse: ANIMAL_TASK_WH